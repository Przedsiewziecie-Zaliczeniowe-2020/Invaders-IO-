const Ship = require('./ship');


test('ship exist correctly', () => {
    const testShip=new Ship();
const spy=jest.spyOn(testShip,);
spy.mockReturnValue();
    expect(typeof testShip.y).toBe(3);
});

